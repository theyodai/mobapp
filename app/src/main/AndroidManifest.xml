<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.photolent">

    <!-- Добавление разрешения для доступа к интернету -->
    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Photolent"
        tools:targetApi="31">

        <!-- Активность по умолчанию -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Активность для отображения фотогалереи -->
        <activity
            android:name=".PhotoGalleryActivity"
            android:label="Photo Gallery"
            android:theme="@style/Theme.AppCompat">
            <!-- Добавьте здесь любые другие настройки активности -->
        </activity>

    </application>

</manifest>
